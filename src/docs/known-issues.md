# Известные проблемы и технический долг

Это реестр подтвержденных рисков. Он не заменяет GitHub Issues, но дает агенту обязательный контекст до изменения системы.

## Активные

| ID | Приоритет | Проблема | Риск | Следующее действие |
|---|---|---|---|---|
| KI-001 | P0 | Не все JSON-RLS проверяют `User.asset_ids`; часть правил сравнивает только `user.asset_id` | Ответственный за несколько активов может не увидеть разрешенные данные либо архитектура начнет полагаться на UI | Спроектировать безопасный backend-контур чтения/записи или согласовать модель одного asset в RLS |
| KI-002 | P0 | Пересборка `OilLifecycle` после desktop/mobile изменения события требует приемки на production-подобных данных | Неверная привязка проб и длительности масла | Прогнать create/update/delete/backdate сценарии и сохранить результаты |
| KI-003 | P0 | План-факт замены масла связан кодом, но не имеет зафиксированного acceptance-набора | Дашборды могут неверно показывать просрочку или факт | Создать таблицу сценариев по датам и моточасам |
| KI-004 | P1 | Руководство и UI поддерживают несколько активов, но безопасность распределена между RLS и backend | Несогласованное поведение страниц и операций | Составить матрицу CRUD по сущностям и ролям |
| KI-005 | P1 | Не все формы проверены на обязательный `buildPayload + useSaveMutation` | Возможны тихие несохранения и отсутствие сообщения об ошибке | Провести отдельный grep-аудит форм и мигрировать оставшиеся |
| KI-006 | P1 | В удаленной БД могут оставаться legacy-записи `SamplingPoint` | Разработчики могут принять их за актуальный источник | После проверки деплоя схемы удалить отдельной admin-операцией |
| KI-007 | P2 | Не определен жизненный цикл ручной корректировки `SamplingSchedule` | Следующая проба может неожиданно перезаписать план | Утвердить вариант в DEC-016 и реализовать серверное правило |
| KI-008 | P2 | `notification_enabled` существует без механизма доставки | UI создает ложное ожидание уведомлений | Выбрать первые сценарии и канал в следующем релизе |
| KI-009 | P2 | Схема `ThresholdRule` содержит legacy scope-поля | Агент может вернуть устаревшую привязку к типу/узлу | Не использовать их без нового решения; позже выполнить миграцию схемы |
| KI-010 | P2 | Дублированный `recalcUnit` в backend-функциях | Исправление одной копии может рассинхронизировать поведение | До доказанного function-to-function обновлять все копии и проверять diff |
| KI-011 | P2 | `npm run typecheck` не проходит на текущем baseline | Ошибки типов скрывают новые регрессии и создают шум при приемке | Отдельно настроить JSX-типы, декларации Base44 SDK и постепенно закрыть существующий список |
| KI-012 | P1 | Для `client_admin` нужна безопасная backend-функция приглашения пользователей франчайзи | Прямой доступ к `User.list/update` может раскрыть лишние учетные записи или дать слишком широкие права | Спроектировать `inviteScopedUser` с проверкой `client_id` и разрешенных ролей |
| KI-013 | P0 | Платформа расследований не развёрнута на сервере: поддомен, секреты и первый прогон Action не выполнены | Код и проверки готовы, но продукта на живых данных пока нет | Направить `investigation.regattayg.space` на сервер, добавить секреты, запустить Action «Deploy investigation platform» |
| KI-047 | закрыт | Симулятор расследований и бенчмарк §51–§52: учебное дело со скрытой истиной, Case Director, четырнадцать метрик с защитными порогами | — | Библиотека пока из одного учебного дела; пополнение — отдельная работа |
| KI-048 | P2 | Результаты бенчмарка видны только в командной строке | Владелец не может сравнить прогоны из интерфейса | Сделать экран прогонов симулятора со сравнением по метрикам |
| KI-049 | P2 | В библиотеке одно учебное дело | Бенчмарк на одном сценарии показывает меньше, чем на десяти | Добавить дела с иным исходом: невиновный подозреваемый, системная ошибка без причастных, сговор двоих |
| KI-015 | закрыт | Реализованы все 18 агентов из §22–§40 ТЗ | — | Дальнейшее развитие — Phase 3: библиотека дел, симулятор, бенчмарк |
| KI-016 | P1 | Векторный поиск технически готов (pgvector, `semanticSearch`), но эмбеддинги не строятся: функция построения вектора не подключена | Поиск похожих дел и claim не работает; `semanticSearch` отказывает явно, чтобы не подменять качество молча | Подключить построение эмбеддингов и наполнить методологическое пространство знаний |
| KI-017 | закрыт | Исполнитель очереди реализован в контейнере API с захватом через `for update skip locked`, отсрочкой повторов и остановкой на утверждении человеком | — | Обработчики транскрипции, разбора документов и отчёта отказывают явно и ждут реализации |
| KI-018 | P0, скрипт готов | Развёртывание идёт под root по паролю из GitHub Secrets | Компрометация секрета даёт полный контроль над сервером с материалами расследований | Владельцу создать пару ключей, добавить `DEPLOY_SSH_PRIVATE_KEY` и `DEPLOY_SSH_PUBLIC_KEY`, запустить Action «SETUP - harden investigation server access» |
| KI-019 | P1 | Резервное копирование реализовано и проверяет копию восстановлением, но копии остаются на том же сервере | Отказ диска уносит и данные, и копии | Владельцу выбрать внешнее хранилище; копии не выгружать в юрисдикцию, недопустимую для персональных данных |
| KI-020 | P1 | Шифрование при хранении, требуемое §60 ТЗ, не включено | Материалы внутренних расследований лежат на диске открыто | Включить шифрование тома с оригиналами и каталога базы |
| KI-021 | P1 | Юридический контур продажи внешним клиентам не готов: нет договора об обработке персональных данных и описанного порядка экспорта | Технически `deleteTenantData` и каскады готовы, продавать без юридической части нельзя | Подготовить договор и регламент экспорта и удаления данных клиента |
| KI-022 | P2 | `RelationalGraphRepository.buildCaseGraph` собирает граф дела в памяти приложения | На деле с тысячами утверждений это станет узким местом | Переписать сборку графа на SQL с рекурсивным обходом; интерфейс `GraphRepository` менять не нужно |
| KI-023 | закрыт | Экран участника реализован как самодостаточная страница по подписанной ссылке | — | Голосовой ответ и вложения появятся вместе с транскрипцией и разбором документов |
| KI-025 | частично закрыт | Голосовые ответы приняты и расшифровываются на своём сервере; вложения файлов от участника ещё не поддержаны | Материалы, на которые ссылается участник, приходится запрашивать отдельно | Добавить приём вложений после разбора документов (агент 04) |
| KI-026 | P2 | Смена пароля пользователя доступна только повторным запуском Action создания организации | Владелец не может сменить свой пароль сам | Сделать экран управления пользователями организации со сменой пароля и приглашениями |
| KI-027 | P2 | Отчёт хранится структурой в базе, но не выгружается в документ | Итог расследования нельзя передать за пределы системы в привычном виде | Сделать выгрузку в PDF или DOCX по разделам §40 |
| KI-028 | P1 | Качество распознавания на сервере не проверено на живой русской речи | Модель подбирается по памяти сервера автоматически; на слабой машине выберется base с заметными ошибками | После развёртывания записать пробный ответ и сравнить расшифровку с речью; при необходимости задать WHISPER_MODEL явно |
| KI-029 | P2 | Расшифровка не сохраняет метки времени сегментов | Утверждение из голосового ответа ссылается на текст целиком, а не на место в записи | Передавать сегменты в source_locator при извлечении утверждений |
| KI-030 | P1 | Сканы и изображения не распознаются: разбор отказывает явно | Материал, пришедший фотографией документа, приходится читать глазами | Подключить OCR; распознавание речи на сервере уже есть, OCR ложится тем же путём |
| KI-031 | закрыт | Рабочее место следователя реализовано: обзор, хронология, матрица доказательств, карта противоречий, доска версий, движение средств, выводы и отчёт | — | Действия с экрана (запуск анализа, утверждение выводов) пока выполняются через API |
| KI-024 | закрыт | Хронология сопоставляет события по набору утверждений, а не по тексту описания; мягко удалённое событие больше не «оживает» скрытно | — | — |
| KI-033 | закрыт | Вопросы интервью открываются участнику при выдаче ссылки, чувствительные — отдельным решением человека с обоснованием | — | Найдено независимой проверкой: участник открывал ссылку и видел пустой список, потому что статус `draft` никто не снимал |
| KI-034 | закрыт | Граница дела внутри организации закрыта в слое хранения: `get` не видит объект чужого дела, `update` больше не проставляет `case_id` из области видимости | — | Прежде запрос к объекту чужого дела по идентификатору переносил объект в дело вызывающего |
| KI-035 | закрыт | Утверждение отправки интервью привязано к делу и перечню интервью; появился маршрут запроса и действия на экране | — | Прежде любое утверждённое `interview_dispatch` в организации открывало ссылки участникам всех дел |
| KI-036 | закрыт | Лимит персональной ссылки считает отправленные ответы, а не открытия страницы | — | Прежде участник упирался в лимит на середине интервью и видел «ссылка недействительна» |
| KI-037 | закрыт | Вход ограничен по частоте, проверка пароля выполняется и для несуществующего адреса | — | Разница во времени ответа снова делала форму входа справочником сотрудников |
| KI-038 | закрыт | Границы недоверенного блока обезврежены и помечены случайной меткой запуска | — | Документ мог структурно закрыть блок данных и открыть секцию инструкций |
| KI-039 | закрыт | `trustProxy` ограничен адресами своей сети; чтение файла проверяет нормализованный путь | — | Клиент мог назначать себе адрес заголовком; проверка пути сравнивала строки |
| KI-040 | закрыт | DOCX разбирается; XLSX, устаревший Office и нераспознанный двоичный формат отклоняются с объяснением | — | Прежде docx декодировался как UTF-8, и агент извлекал утверждения из мусора без единой ошибки |
| KI-041 | закрыт | Решённый запрос утверждения нельзя переутвердить; решение принимает человек, а не агент; запрашивающим записывается автор предложения | — | Разделение обязанностей включается настройкой `REQUIRE_SEPARATE_APPROVER=1` |
| KI-042 | закрыт | Обоснование смены стадии записывается в журнал; переход больше не затирает причину удаления дела | — | — |
| KI-043 | закрыт | Пароль владельца организации уходит на сервер стандартным вводом, а не аргументом `docker run` | — | Аргументы видны в `ps aux` любому, кто есть на сервере |
| KI-044 | P2 | Нет маршрута для получения оригинала приобщённого материала | Ссылка на позицию в источнике ведёт к документу, который нельзя открыть из системы | Добавить выдачу оригинала по идентификатору источника с проверкой прав и записью в журнал доступа |
| KI-045 | P2 | Номер дела вычисляется счётчиком вне транзакции | Одновременное создание двух дел даёт ошибку записи вместо понятного ответа | Перевести нумерацию на последовательность внутри транзакции |
| KI-046 | P2 | Таблицы XLSX не разбираются | Учётные выгрузки — частый материал; приходится просить CSV | Разобрать OOXML-таблицу тем же читателем ZIP, что уже используется для DOCX |
| KI-032 | закрыт | Действия доступны с экранов: разбор заявления, планирование, аналитический цикл, классификация выводов, подготовка интервью, решения по утверждениям, закрытие противоречий, утверждение выводов и выпуск отчёта | — | Выдача ссылки участнику пока через API: ссылка показывается один раз и требует отдельного экрана |

## Недостающие контрольные документы

После этого аудита остаются полезными, но еще не созданными:

- матрица ролей x сущностей x CRUD;
- реестр страниц и доступных ролей;
- формализованный acceptance-набор для maintenance/lifecycle/plan-fact;
- журнал выполненных production-проверок с датой, средой и результатом.

## Закрытие проблемы

При закрытии:

1. добавить ссылку на commit/PR или тестовый отчет;
2. обновить `ROADMAP.md`;
3. при изменении продуктового решения обновить `decisions.md`;
4. не удалять полезный контекст причины и способа проверки.
